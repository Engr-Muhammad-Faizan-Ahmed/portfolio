const header=document.querySelector('.header');const menuBtn=document.getElementById('menuBtn');const navLinks=document.getElementById('navLinks');const navItems=document.querySelectorAll('.nav-links a');const sections=document.querySelectorAll('section[id]');const revealItems=document.querySelectorAll('.reveal');window.addEventListener('scroll',()=>{header.classList.toggle('scrolled',window.scrollY>40);updateActiveLink();});menuBtn.addEventListener('click',()=>navLinks.classList.toggle('open'));navItems.forEach(i=>i.addEventListener('click',()=>navLinks.classList.remove('open')));function updateActiveLink(){let c='';sections.forEach(s=>{const t=s.offsetTop-150,h=s.offsetHeight;if(window.scrollY>=t&&window.scrollY<t+h)c=s.getAttribute('id')});navItems.forEach(i=>{i.classList.remove('active');if(i.getAttribute('href')===`#${c}`)i.classList.add('active')})}const observer=new IntersectionObserver(e=>{e.forEach(x=>{if(x.isIntersecting)x.target.classList.add('show')})},{threshold:.13});revealItems.forEach(i=>observer.observe(i));
